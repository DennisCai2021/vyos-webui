#!/bin/bash
set -e

case "$1" in
    install|upgrade)
        # Create user if not exists
        if ! getent passwd vyos > /dev/null; then
            # VyOS user should already exist on VyOS systems
            true
        fi
        ;;
esac

exit 0
